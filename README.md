# leaflet map using react
  -  clone this repo
  -  change directory to leaflet-map i.e  `cd leaflet-map`
  -  enter the command `npm install`
  -  enter `npm start`
  -  by default app will be opened up in localhoast:3000 .
